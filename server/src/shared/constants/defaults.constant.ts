/**
 * Valores por defecto usados como fallback cuando la clínica
 * no tiene configurada una zona horaria o nombre.
 */
export const DEFAULT_TIMEZONE = 'America/Lima';
export const DEFAULT_CLINIC_NAME = 'MediClick';
